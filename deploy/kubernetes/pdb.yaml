apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: orchestrator-api
spec:
  # Matches the deployment baseline so voluntary disruptions cannot drop the
  # service to a single replica.
  minAvailable: 2
  selector:
    matchLabels:
      app: orchestrator-api
